import type { DataRequest } from "@/lib/types";

// No Data Requests exist yet in the seed data — Stage 4 is what
// creates the first one (Coca-Cola requesting selected fields on PET
// Bottle 500ml from PET Solutions GmbH). See DOMAIN.md §3 for the
// DataRequest shape and AGENTS.md §8 for the status enum.
export const dataRequests: DataRequest[] = [];
