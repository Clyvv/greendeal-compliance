import type { DataApproval } from "@/lib/types";

// No Data Approvals exist yet — Stage 5 creates the first one, when
// PET Solutions GmbH approves (a subset of) the Stage 4 request for
// PET Bottle 500ml. See DOMAIN.md §3 for the DataApproval shape.
export const dataApprovals: DataApproval[] = [];
